# Testing dependencies for FaaS platform
# Install with: pip install -r requirements-dev.txt

# vmtest - Run tests in a lightweight VM with real kernel
# Note: vmtest must be installed from GitHub (not on PyPI)
# Install with: pip install git+https://github.com/danobi/vmtest.git
# For now, this is optional and tests will skip if not available

# pytest - Testing framework
pytest>=7.4.0
pytest-xdist>=3.3.0  # Parallel test execution
pytest-timeout>=2.1.0  # Test timeouts
pytest-cov>=4.1.0  # Code coverage

# Testing utilities
requests>=2.31.0  # For HTTP testing
